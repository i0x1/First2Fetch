# Root-level environment variables
# This file is used by docker-compose.yml and other root-level tooling
# Add any shared environment variables here that are needed across multiple services

# Example: Node environment
# NODE_ENV=development

# Example: Shared API keys or configuration
# Add any root-level configuration here
