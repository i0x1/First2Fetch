{
  "name": "@first2apply/backend",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "command -v deno >/dev/null 2>&1 && (cd supabase/functions && deno check --config deno.json **/*.ts) || echo 'Skipping deno check: deno not found'",
    "stop": "npx supabase stop",
    "start": "npx supabase start",
    "dev": "npx supabase functions serve",
    "prettier": "npx prettier --check \"supabase/functions/**/*.ts\""
  },
  "author": "BeastX Industries",
  "license": "MIT",
  "dependencies": {
    "@first2apply/core": "workspace:*"
  },
  "devDependencies": {}
}
